<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.healthyme.mapper.boardMapper">
	
	<insert id="insertPost">
		insert into post (post_title, post_content, post_writer)
		values(#{postTitle},#{postContent}, #{postWriter})
	</insert>

	<select id="selectList" resultType="PostVO">
		selcet *
		from post
		where category_idx = #{categoryIdx}
	</select>
</mapper>